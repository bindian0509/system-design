apiVersion: v1
kind: ConfigMap
metadata:
  name: url-shortener-config
  namespace: url-shortener
  labels:
    app: url-shortener
data:
  aws_region: "us-east-1"
  base_url: "https://short.io"
  log_level: "info"
  cache_ttl: "86400"
  rate_limit_rps: "100"
