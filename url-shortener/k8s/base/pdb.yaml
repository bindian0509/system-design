apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: url-shortener
  namespace: url-shortener
  labels:
    app: url-shortener
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: url-shortener
